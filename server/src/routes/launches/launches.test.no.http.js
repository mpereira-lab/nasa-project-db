describe('Test GET /launches', () => {
  test('It should respond with 200 success', ()=> {
    const response = 200;
    expect(response).toBe(200);
  })  
})

describe('Test POST /launches', ()=>{
    test('It should respond with 200 success', () => {

    })

    test('It should respond with 400 missing required property', () => {})
    test('It should respond with 400 invalid date', () => {})

})